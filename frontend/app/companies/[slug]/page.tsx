import Link from "next/link";
import { BriefcaseBusiness, ExternalLink, MapPin, ShieldCheck } from "lucide-react";
import { safeGet } from "@/lib/api";
import type { CompanyProfile, Opportunity } from "@/types";
import OpportunityCard from "@/components/opportunities/OpportunityCard";

export const dynamic="force-dynamic";
export default async function CompanyPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const company=(await safeGet<CompanyProfile|null>(`/opportunities/companies/${slug}/`,null)).data;if(!company)return <div className="container-app py-16"><div className="card p-10 text-center">Entreprise indisponible.</div></div>;const opp=(await safeGet<Opportunity[]>(`/opportunities/companies/${slug}/opportunities/`,[])).data||[];return <main className="min-h-screen bg-[#f7f8fa]"><section className="border-b border-gray-200 bg-white"><div className="container-app py-10"><div className="flex flex-col gap-5 sm:flex-row sm:items-start"><div className="grid h-20 w-20 shrink-0 place-items-center overflow-hidden rounded-2xl bg-gray-100 text-gray-400">{company.logo?<img loading="lazy" decoding="async" src={company.logo} alt="" className="h-full w-full object-cover"/>:<BriefcaseBusiness size={30}/>}</div><div><div className="flex flex-wrap items-center gap-2"><h1 className="text-3xl font-extrabold">{company.name}</h1><span className="badge bg-emerald-50 text-emerald-700"><ShieldCheck size={12}/> Vérifiée</span></div><p className="mt-2 text-sm text-gray-500">{company.sector||"Entreprise"}</p><p className="mt-2 flex items-center gap-1 text-sm text-gray-500"><MapPin size={14}/>{company.city?`${company.city}, `:""}{company.country}</p>{company.website_url&&<a href={company.website_url} target="_blank" rel="noreferrer" className="btn-outline mt-4 !py-2 !text-xs"><ExternalLink size={13}/> Site web</a>}</div></div>{company.description&&<p className="mt-7 max-w-4xl whitespace-pre-wrap text-sm leading-7 text-gray-600">{company.description}</p>}</div></section><section className="container-app py-10"><h2 className="mb-5 text-xl font-bold">Opportunités ouvertes ({opp.length})</h2>{opp.length?<div className="grid gap-5 lg:grid-cols-2">{opp.map(o=><OpportunityCard key={o.id} opportunity={o}/>)}</div>:<div className="card p-10 text-center text-gray-500">Aucune opportunité ouverte pour le moment.</div>}<div className="mt-8"><Link href="/opportunities" className="font-semibold text-brand-700">← Toutes les opportunités</Link></div></section></main>}
